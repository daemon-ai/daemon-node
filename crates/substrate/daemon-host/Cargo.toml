[package]
name = "daemon-host"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Phase-3 scope: resident-service supervision plus the §17<->management protocol translation and
# the real engine. The remaining deferred deps (daemon-provision, daemon-credentials,
# daemon-telemetry, daemon-transport) land with provisioning / credential authority / cross-node.
[dependencies]
daemon-common = { workspace = true }
daemon-protocol = { workspace = true }
daemon-supervision = { workspace = true }
daemon-core = { workspace = true }
daemon-store = { workspace = true }
daemon-activation = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
dashmap = { workspace = true }
async-trait = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
