[package]
name = "daemon-host"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Phase-2 scope: resident-service supervision over the durable substrate. The deferred deps
# (daemon-protocol, daemon-supervision, daemon-core, daemon-provision, daemon-credentials,
# daemon-telemetry) return in phase 3 when the §17<->management translation and real engine land.
[dependencies]
daemon-common = { workspace = true }
daemon-store = { workspace = true }
daemon-activation = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
dashmap = { workspace = true }
async-trait = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
