/target
/result
**/*.rs.bk
Cargo.lock.bak
.DS_Store
# Local research/scratch notes, path-anchored so they do NOT match the bundled skill
# `crates/skills/daemon-skills/bundled/research` (which must stay tracked so worktrees seed it).
/research/
/docs/research/
