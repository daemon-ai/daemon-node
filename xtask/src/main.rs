//! `xtask` — repo automation (link sweeps, codegen, CI helpers).

#![forbid(unsafe_code)]

fn main() -> anyhow::Result<()> {
    // TODO: subcommands (e.g. `link-check` for the docs corpus).
    Ok(())
}
