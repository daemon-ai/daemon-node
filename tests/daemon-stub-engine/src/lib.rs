//! `daemon-stub-engine` — a minimal §17-speaking engine for exercising the substrate.
//!
//! Lets `daemon-conformance` drive activation/host behavior without the real `daemon-core`.

#![forbid(unsafe_code)]

// TODO: deterministic stub engine speaking §17 for conformance tests.
