//! `daemon-cli` — operator CLI for inspecting/steering a running daemon.

#![forbid(unsafe_code)]

fn main() -> anyhow::Result<()> {
    // TODO: operator commands over the management protocol.
    Ok(())
}
