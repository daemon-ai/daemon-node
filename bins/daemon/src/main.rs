//! `daemon` — the host binary that assembles an engine, its host, tools, and orchestration.

#![forbid(unsafe_code)]

fn main() -> anyhow::Result<()> {
    // TODO: wire host + engine + tools + orchestration and run.
    Ok(())
}
